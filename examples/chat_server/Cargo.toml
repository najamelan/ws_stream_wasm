# Auto-generated from "Cargo.yml"
[[bin]]
name = "server"
path = "src/server.rs"

[dependencies]
bytes = "^0.4"
chrono = "^0.4"
failure = "^0.1"
flexi_logger = "^0.14"
futures_codec = "^0.2"
log = "^0.4"
rand = "^0.7"
regex = "^1"
serde_cbor = "^0.10"

[dependencies.async_runtime]
package = "naja_async_runtime"
version = "^0.3"

[dependencies.futures-preview]
features = ["async-await"]
version = "^0.3.0-alpha"

[dependencies.serde]
features = ["derive"]
version = "^1"

[lib]
name = "chat_format"
path = "src/lib.rs"

[package]
authors = ["Naja Melan <najamelan@autistici.org>"]
description = "An example for using websockets in rust WASM."
edition = "2018"
name = "chat_server"
repository = "https::/github.com/najamelan/ws_stream"
version = "0.1.0"

[target]
[target."cfg(not( target_arch = \"wasm32\" ))"]
[target."cfg(not( target_arch = \"wasm32\" ))".dependencies]
lock_api = "^0.3"
once_cell = "^1.0.0-pre"
parking_lot = "^0.9"
pin-utils = "^0.1.0-alpha"

[target."cfg(not( target_arch = \"wasm32\" ))".dependencies.locks]
package = "future-parking_lot"
version = "^0.3.2-alpha"

[target."cfg(not( target_arch = \"wasm32\" ))".dependencies.warp]
default-features = false
features = ["websocket"]
version = "^0.1"

[target."cfg(not( target_arch = \"wasm32\" ))".dependencies.ws_stream]
path = "../../../ws_stream"
